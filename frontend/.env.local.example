# Dev: leave empty so the browser uses the Next.js proxy on :3001 → backend :8001.
NEXT_PUBLIC_API_URL=

# Next.js dev proxy target (server-side only).
BACKEND_URL=http://127.0.0.1:8001

# Production example (direct API, no proxy):
# NEXT_PUBLIC_API_URL=https://your-api.example.com
